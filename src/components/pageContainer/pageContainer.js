import React from 'react'
import { pageSection } from './pageContainerStyles'

const pageContainer = () => {
    return (
        <section style={pageSection}>
            <h1>Hello World</h1>
        </section>
    )
}

export default pageContainer